apiVersion: v1
kind: Service
metadata:
  name: mcpvectordb
  namespace: default
spec:
  selector:
    app: mcpvectordb
  ports:
    - name: mcp
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30800
  type: NodePort
